<div class="container">
  <h1>Discover out items</h1>
  <h2>What you can rent in your SmartNeighbourhood</h2>

  <div class="d-flex flex-wrap">
    <% @items.each do |item| %>
      <div class="card-item">
          <% if item.photo.attached? %>
            <%= cl_image_tag item.photo.key, crop: :fill %>
          <% else %>
            <img src='https://source.unsplash.com/800x450/?<%= item.name %>' alt='<%= item.name %>' >
          <% end %>
        <div class="card-item-infos">
          <h2><%= item.name %></h2>
          <h1><%= item.location %></h1>
          <div class="d-flex">
            <h2><i class="card-item-pricing"></i></i><%= item.price %> per day</h2>
          </div>
          <%= link_to 'more info', item_path(item), class:"btn-primary" %>
        </div>
      </div>
    <% end %>
  </div>

</div>




<!--


  <div class="d-flex flex-wrap">
    <% @items.each do |item| %>
      <div class="card">
        <h3><%= item.name %></h3>
        <p><%= item.location %></p>
        <div class="d-flex">
          <p><i class="fas fa-euro-sign"></i></i><%= item.price %></p>
        </div>
        <%= link_to 'more info', item_path(item) %>
      </div>
    <% end %>

  </div>

</div>


<div class="card-item">
  <img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/greece.jpg" />
  <div class="card-item-infos">
    <div>
      <h2>Item name</h2>
      <h1>Distance in 0.0 km</h1>
    </div>
    <h2 class="card-item-pricing">€ 99.99 per day</h2>
  </div>
</div> -->
